import React from 'react';

import ErrorMsg from '../ErrorMsg/ErrorMsg';

const submitButton = (props) => {

    // if (props.errormsg !== null) {
    //     return (<Popup
    //         trigger={<input type="submit" name="" value={props.name} />}
    //         on='hover'
    //         mouseLeaveDelay={350}
    //         mouseEnterDelay={0}
    //         defaultOpen="true"
    //     >
    //         <ErrorMsg errormsg={props.errormsg} />
    //     </Popup>);
    // }
    // //else
    // return (<input type="submit" name="" value={props.name} />);
}

export default submitButton;